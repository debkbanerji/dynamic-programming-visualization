{
    "name": "dynamic-programming-visualization",
    "version": "0.0.0",
    "main": "server.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "node server.js",
        "postinstall": "cd source && npm install && cd .. && npm run build",
        "build": "cd source && ng build --prod --output-path=../docs && cd .."
    },
    "author": "Deb Banerji",
    "license": "MIT",
    "dependencies": {
        "body-parser": "^1.17.2",
        "express": "^4.14.0",
        "path": "^0.12.7"
    }
}
